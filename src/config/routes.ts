interface Routes {
    home: string;
    homeTable: string;
    login: string;
    boards: string;
    templates: string;
    works: string;
}

const routes:Routes = {
    home: '/',
    homeTable: '/home',
    login:'/login',
    boards:'/boards',
    templates:'/templates',
    works:'/works'

};

export default routes;
